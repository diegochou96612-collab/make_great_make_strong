# 專案除錯紀錄

> 專案：PetMedApp（動物飼養助手）  
> 環境：Android Studio / AGP 9.3.1 / Kotlin 2.2.x / KSP / Room

---

## 問題一：KSP 版本號找不到

**錯誤訊息**
```
Plugin [id: 'com.google.devtools.ksp', version: '2.2.10-1.0.28'] was not found
```

**原因**  
KSP 在 2.x 世代改變了版本號格式。  
舊格式：`<kotlin版本>-1.0.x`（例如 `1.9.10-1.0.13`）  
新格式：`<kotlin版本>-2.0.x`（例如 `2.2.10-2.0.2`）  
填入舊格式的版本號，Maven 找不到對應的 artifact。

**解法**  
到 [github.com/google/ksp/releases](https://github.com/google/ksp/releases) 確認對應 Kotlin 版本的正確 KSP 版本號。  
Kotlin `2.2.10` 對應 `ksp = "2.2.10-2.0.2"`。

---

## 問題二：KSP 與 AGP Built-in Kotlin 不相容

**錯誤訊息**
```
KSP is not compatible with Android Gradle Plugin's built-in Kotlin.
Please disable by adding android.builtInKotlin=false to gradle.properties
```

**原因**  
AGP 9.x 引進了「Built-in Kotlin」機制，將 Kotlin 整合進 AGP 本身，不再需要獨立的 `kotlin-android` plugin。  
KSP `2.2.x` 版本尚未支援這個新機制，因此與 AGP 9.x 產生衝突。

**錯誤的解法（引發後續問題）**  
依照錯誤提示在 `gradle.properties` 加入 `android.builtInKotlin=false`，  
這個做法強制停用 Built-in Kotlin，並要求手動加回 `kotlin-android` plugin，  
但反而引發下方的問題三與問題四。

**正確解法**  
升級 KSP 至 `2.3.x`（例如 `2.3.10`），此版本原生支援 AGP Built-in Kotlin，  
不需要任何 workaround，`gradle.properties` 保持乾淨即可。

---

## 問題三：kotlin.sourceSets DSL 限制

**錯誤訊息**
```
Using kotlin.sourceSets DSL to add Kotlin sources is not allowed with built-in Kotlin.
Solution: Use android.sourceSets DSL instead.
```

**原因**  
AGP 9.x 在啟用 Built-in Kotlin 時，禁止使用舊式的 `kotlin.sourceSets` DSL。  
KSP `2.2.x` 的程式碼產生器仍使用舊 DSL 加入產生的 source，因此觸發此限制。

**臨時解法（後來撤回）**  
加入 `android.disallowKotlinSourceSets=false` 暫時關閉限制。

**根本解法**  
同問題二，升級 KSP 至 `2.3.x` 後此問題自動消失。

---

## 問題四：kotlin-android Plugin 已在 classpath，版本衝突

**錯誤訊息**
```
The request for this plugin could not be satisfied because the plugin is already
on the classpath with an unknown version, so compatibility cannot be checked.
```

**原因**  
AGP Built-in Kotlin 已將 Kotlin 放入 classpath，  
再用 `alias(libs.plugins.kotlin.android)` 並指定版本號套用時，Gradle 找到兩份相同 plugin，  
無法比對版本相容性，直接報錯。

**臨時解法（後來撤回）**  
改用 `kotlin("android")` 不帶版本號的寫法，讓 Gradle 沿用已在 classpath 上的版本。  
但這又觸發問題五。

---

## 問題五：BaseExtension ClassCast 錯誤

**錯誤訊息**
```
class ApplicationExtensionImpl cannot be cast to class BaseExtension
```

**原因**  
AGP 9.x 移除了舊的 `BaseExtension` API，改用 `ApplicationExtension`。  
舊版 `kotlin-android` plugin 內部仍試圖將 AGP Extension 強制轉型為 `BaseExtension`，  
導致 ClassCastException。

**根本解法**  
撤回所有 workaround（`android.builtInKotlin=false`、`android.disallowKotlinSourceSets=false`、`kotlin("android")`），  
直接升級 KSP 至 `2.3.x`，讓 AGP Built-in Kotlin 正常運作。

---

## 問題六：Room + KSP 的 `unexpected jvm signature V`

**錯誤訊息**
```
[ksp] java.lang.IllegalStateException: unexpected jvm signature V
```

**原因**  
Room `2.6.x` 的 Annotation Processor 在 KSP 2.x 環境下，  
無法正確處理回傳 `Unit`（JVM signature 為 `V` 即 void）的 `suspend` 函式。  
受影響的是 DAO 中的 `update`、`delete` 等無回傳值的方法。

**解法**  
升級 Room 至 `2.7.x`（`room = "2.7.1"`）。  
Room `2.7.0` 起針對 KSP 2.x 做了專項修正。

---

## 問題七：TextField 無法輸入中文

**現象**  
`HospitalSearchScreen` 的搜尋欄只能輸入英文，輸入中文時組字過程會被中斷。

**原因**  
使用 `String` 型別的 state 搭配 `OutlinedTextField` 時，  
每次 `onValueChange` 都會更新 state 並觸發 Compose recomposition。  
中文 IME 在組字（composition）階段需要連續的輸入流，  
recomposition 會打斷這個流程，導致組字失敗。

**解法**  
將 state 型別從 `String` 改為 `TextFieldValue`：

```kotlin
// 改前
var searchQuery by remember { mutableStateOf("") }

// 改後
var searchQuery by remember { mutableStateOf(TextFieldValue("")) }
```

`TextFieldValue` 除了儲存文字外，還保留游標位置（`selection`）和組字範圍（`composition`），  
讓 IME 在 recomposition 後能正確還原組字狀態。

---

## 總結：核心教訓

| 問題根源 | 說明 |
|----------|------|
| **版本號格式變更** | KSP 2.x 改變版本命名，需查官方 releases 確認 |
| **AGP 9.x 重大變更** | Built-in Kotlin 改變了 Kotlin plugin 的整合方式，KSP 需 2.3.x+ 才支援 |
| **依賴版本不同步** | Room 2.6.x 未針對 KSP 2.x 修正，需升至 2.7.x |
| **逐步 workaround 反效果** | 依照錯誤提示逐步加 workaround，反而使問題更複雜，應優先考慮升版 |
| **中文 IME 處理** | Compose TextField 處理多字節輸入需用 `TextFieldValue` 而非 `String` |

---

## 建議的起始版本組合（AGP 9.x 專案）

```toml
agp = "9.3.1"
kotlin = "2.2.20"
ksp = "2.3.10"
room = "2.7.1"
```
